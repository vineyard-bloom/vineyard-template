import { FullConfig } from '../../config/config-types';
export declare function createBackingServices(config: FullConfig, overrides: Partial<BackingServices>): BackingServices;
export interface BackingServices {
}
