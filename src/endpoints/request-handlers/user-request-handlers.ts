import { requestTypes, responseTypes } from './api-types'

export const userRequestHandler = {
  loginUser: async (req: requestTypes.LoginUserRequest): Promise<responseTypes.LoginUserResponse> => {
    return 
  }
}