import { FullConfig } from '../config/config-types'

export function createBackingServices (config: FullConfig, overrides: Partial<BackingServices>): BackingServices {
  return {}
}

export interface BackingServices {

}
